[$HOME/.config/vcsh/repo.d/secret.git]
order = 10
# NOTE: put the correct url for your repository
checkout = vcsh clone -b secret https://github.com/DNajjarzade/vcsh_mr_template.git ~/.config/secret && \
vcsh mr branch --track bash origin/secret
